# BMRS / NESO data cache (45 MB) IS committed, so a fresh clone + reproduce_all.py
# reproduces every number offline, deterministically. Re-fetched live otherwise.

# Pipeline outputs (regenerated by reproduce_all.py; the small canonical CSVs
# -- annual_sample_*, neso_daily_*, sensitivity_*, pg_* -- are kept/committed.)
results_b6_*.csv
nonconvex_static.csv
reproduce_all.log

# Manuscript / paper artifacts — this is a code-only replication repo
cost-of-national-pricing.tex
cost-of-national-pricing.pdf
references.bib
fig_gbmap.pdf
fig_gbmap.png
highlights.txt

# Internal — NOT for the public repository
cover_letter.*
*.docx
not-submission-related/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.spl
*.synctex.gz
*.fdb_latexmk
*.fls
*.toc

# OS / editor
.DS_Store
.vscode/
.idea/
